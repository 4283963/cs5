# 服务端口
PORT=3001

# MySQL 数据库连接
# 若未配置 DB_HOST 或连接失败，后端会自动降级为内存种子数据，保证接口可用
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cs5_auth

# 设为 true 强制使用内存种子数据（不连接 MySQL）
DB_USE_MOCK=false
